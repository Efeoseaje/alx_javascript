// script that prints 3 lines: (like 1-multi_languages.js) but by using an array of string and a loop

const myArray = ['C is fun', 'Pythin is cool', 'JavaScript is amazing'];

for (const element of myArray) {
  console.log(element);
}
